[package]
name = "nexus-test-runner"
version = "0.1.0"
edition = "2021"
description = "Test runner for Nexus sample programs"

[dependencies]
nexus-transpiler-c = { path = "../nexus-transpiler-c" }
nexus-core = { path = "../nexus-core" }

[dev-dependencies]
rayon = "1.10"
